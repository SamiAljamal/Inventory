<h1>Inventory</h1>

@if (@Model.Count == 0)
{
  <p>You have no inventory.</p>
}
<ul>
  @foreach (var item in Model)
  {
    <li> Name: @item.GetName() <br> Description: @item.GetDescription()</li>
  }
</ul>
<a href="/">Add new Inventory</a>
<a href="/inventory/deleted">Delete All Items</a>
